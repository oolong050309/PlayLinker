这是一份为您编写的 PlayLinker 数据分析系统的完整技术文档，涵盖了从数据库底层设计到前端可视化呈现的所有技术细节。

---

# PlayLinker 数据分析系统技术文档

## 1. 系统概述 (Overview)

PlayLinker 数据分析模块（Analytics System）是一个端到端的解决方案，旨在聚合用户跨平台（以 Steam 为核心）的游戏行为数据。系统通过后台自动化采集、核心算法聚合与前端高保真可视化，为用户提供关于**游玩时长**、**题材偏好**、**平台分布**及**成就进度**的深度洞察。

---

## 2. 数据库架构 (Database Architecture)

系统基于 MySQL 构建，核心在于引入了时间维度的快照表以支持趋势分析。

### 2.1 核心数据表：`user_playtime_history`

此表是分析系统的“黑匣子”，记录用户每天的游戏时长快照。

| 字段名 | 类型 | 说明 | 关键特性 |
| --- | --- | --- | --- |
| `history_id` | `BIGINT` | 主键 | 自增 |
| `user_id` | `INT` | 用户外键 | 级联删除 |
| `game_id` | `BIGINT` | 游戏外键 | 级联删除 |
| `platform_id` | `INT` | 平台外键 | 标识数据来源 (如 Steam=1) |
| `playtime_forever` | `INT` | **总时长(分钟)** | **核心数据**：记录当天的累计总时长 |
| `playtime_2weeks` | `INT` | 两周时长(分钟) | Steam 接口原始值，作为参考 |
| `record_date` | `DATE` | 记录日期 | 每日一条，用于时间轴聚合 |
| `created_at` | `DATETIME` | 创建时间 | 审计用 |

* **索引优化**：`UNIQUE KEY (user_id, game_id, platform_id, record_date)` 确保幂等性，防止单日数据重复。

### 2.2 关联支撑表

* **`user_platform_binding`**：存储 OAuth 凭证及加密的 `AccessToken`（Steam API Key），是数据采集的凭证源。
* **`user_platform_library`**：存储当前的最新状态（总时长、最后游玩时间），用于展示 KPI 概览。
* **`user_achievements`**：存储细粒度的成就解锁状态，支撑成就完成率分析。
* **`games`**：提供游戏的元数据（名称、封面图 `header_image`、题材关联）。

---

## 3. 后端架构 (Backend Architecture)

后端基于 ASP.NET Core，分为 **数据采集层 (Service)** 和 **业务逻辑层 (Controller)**。

### 3.1 数据采集服务：`PlaytimeHistoryService`

这是一个 `BackgroundService`（托管服务），负责数据的 ETL（抽取、转换、加载）过程。

* **运行机制**：
* **冷启动触发**：应用启动后 10 秒自动执行一次，便于开发调试。
* **定时调度**：每日凌晨 **02:00** 自动唤醒执行。


* **核心逻辑流**：
1. **凭证获取**：遍历 `UserPlatformBindings` 表，通过 Scope 解析 `ITokenEncryptionService`，解密用户的 `AccessToken` 获取真实 API Key。
2. **数据抓取**：调用 Steam Web API (`GetOwnedGames`) 获取用户全量游戏库及 `playtime_forever`。
3. **映射匹配**：将 Steam AppID 映射为本地系统的 `GameId`。
4. **快照存储**：
* 查询当日是否已存在记录。
* **不存在** -> Insert 新快照。
* **已存在** -> Update 更新快照（支持任务重跑）。





### 3.2 分析控制器：`AnalyticsController`

提供 RESTful API，负责将原始数据转换为可视化图表所需的格式。

* **权限控制**：基于 JWT Token 动态解析 `CurrentUserId`，确保数据隐私。
* **核心接口**：
* **`GET /playtime` (时长趋势)**：
* 算法：查询指定日期范围内的快照 -> 计算 `Data[i].Total - Data[i-1].Total` 得出每日增量。
* 容错：若缺少前一日数据（如统计首日），当日增量记为 0。


* **`GET /genres` (题材偏好)**：
* 算法：`Join` 游戏库与题材表 -> 按 Genre 分组 -> 累加时长 -> 计算百分比权重。


* **`GET /platforms` (平台分布)**：
* 算法：按 `PlatformId` 聚合游戏数量与时长。


* **`GET /achievements` (成就概览)**：
* 算法：统计 `Unlocked=true` 的数量、计算全成就（100%）的游戏数、分析近 7 天的解锁趋势。





---

## 4. 前端架构 (Frontend Architecture)

前端基于 Vue.3 构建，采用高保真 UI 设计，强调数据的可视化呈现。

### 4.1 视图组件：`AnalyticsView.vue`

* **视觉风格**：采用深色主题（Dark Mode），配合霓虹光晕（Glow Effects）、毛玻璃（Glassmorphism）和渐变色，营造科技感。
* **关键模块**：
1. **KPI 仪表盘**：展示总时长、库存量、成就数等核心指标。
2. **游玩热力图 (Heatmap)**：
* **实现**：CSS Grid 布局。
* **逻辑**：仿 GitHub 贡献图，根据每日时长映射 5 个颜色等级，直观展示活跃度。


3. **趋势分析图**：
* **库**：集成 `Chart.js`。
* **特性**：支持“每日/每周”视图切换，采用平滑曲线（Tension 0.4）和渐变填充背景。


4. **富媒体列表**：
* 展示 Top 5 游戏，包含封面图、时长占比进度条和排名徽章（金/银/铜）。


5. **题材雷达图**：展示用户在 RPG、FPS、策略等维度的偏好分布。



### 4.2 API 服务层：`src/api/analytics.js`

* 统一封装 Axios 请求。
* 包含获取各类分析数据、生成报表、下载 PDF 报表的全套接口。

---

## 5. 数据流向闭环 (Data Flow Lifecycle)

1. **采集 (Ingestion)**：
* 后台服务定期唤醒 -> 解密 Key -> 抓取 Steam 数据 -> 存入 MySQL `user_playtime_history`。


2. **处理 (Processing)**：
* 用户请求 API -> Controller 读取历史快照 -> 差分计算每日增量 -> 聚合题材与平台数据。


3. **呈现 (Visualization)**：
* Vue 组件接收 JSON -> 渲染 Canvas 图表与 DOM 列表 -> 用户查看交互式报告。



通过这一整套架构，PlayLinker 实现了从“原始数据积累”到“用户价值洞察”的完整转化。